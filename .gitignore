# Composer dependencies — reinstall with `composer install`
/vendor/

# Runtime user uploads — not source, and grows unbounded
/public/uploads/*
!/public/uploads/.gitkeep

# Secrets — never commit real credentials; .env.copy is the tracked template
.env
.env.*
!.env.copy

# Local DB dump used to seed a new deployment — contains real password hashes/data
/database/mental_health_dump.sql

# Editor/OS junk
.DS_Store
.idea/
.vscode/

# Claude Code local settings
.claude/
